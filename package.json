{
  "name": "athletics-insurance",
  "private": true,
  "workspaces": [
    "worker",
    "web"
  ],
  "scripts": {
    "build": "npm run build -w web",
    "deploy": "npm run build -w web && npm run deploy -w worker"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2"
  }
}
